{
  "private": true,
  "name": "root",
  "author": "Webstack Builders <info@webstackbuilders.com>",
  "version": "0.0.1",
  "engines": {
    "node": ">=15.0.0",
    "npm": ">=7.0.0",
    "yarn": "please-use-npm"
  },
  "scripts": {
    "build": "lerna run build --parallel",
    "build:studio": "lerna bootstrap && cd studio && npm run build",
    "build:web": "lerna bootstrap && (cd studio && SANITY_AUTH_TOKEN=$SANITY_DEPLOY_STUDIO_TOKEN npm run graphql-deploy) && (cd web && npm run build)",
    "deploy:graphql": "lerna run graphql-deploy",
    "dev": "lerna run dev --parallel",
    "format": "lerna run format",
    "link": "lerna link --force-local",
    "lint": "eslint . && lerna run lint",
    "postinstall": "lerna bootstrap",
    "publish": "lerna publish from-git",
    "test": "lerna run test",
    "test:coverage": "lerna run test --coverage",
    "tsc": "tsc",
    "update-browserlist": "npx browserslist@latest --update-db",
    "version:major": "lerna version major",
    "version:minor": "lerna version minor",
    "version:patch": "lerna version patch"
  },
  "devDependencies": {
    "@sanity/cli": "^2.7.4",
    "@types/node": "^16.0.0",
    "@webstackbuilders/eslint-config-company-website": "^0.1.2",
    "@webstackbuilders/prettier-config-company-website": "^0.1.2",
    "gatsby-cli": "^3.8.0",
    "husky": "^7.0.1",
    "lerna": "^4.0.0",
    "typescript": "^4.3.5"
  },
  "eslintConfig": {
    "extends": "@webstackbuilders/eslint-config-company-website",
    "ignorePatterns": [
      "**/web/public/*",
      "**/studio/build/*",
      "**/packages/*"
    ],
    "root": true
  },
  "prettier": "@webstackbuilders/prettier-config-company-website"
}
